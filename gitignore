node_modules/
.env
.env.*
!.env.example
*.log
.DS_Store

# Contiene usernames reales y hashes de contraseñas (scrypt+salt).
# Solo se usa como semilla local la primera vez que no hay DATABASE_URL;
# no debe versionarse en el repositorio.
backend/users.json
